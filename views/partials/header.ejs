<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title><%= title || "Khách sạn Phenikaa" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header class="bg-info py-3 shadow-sm">
    <div class="container d-flex justify-content-between align-items-center">
      <a href="/" class="fw-bold fs-5 text-white text-decoration-none">KHÁCH SẠN PHENIKAA</a>

      <nav class="d-flex align-items-center gap-3">
        <a href="/" class="text-white text-decoration-none fw-semibold">Trang chủ</a>
        <a href="/search" class="text-white text-decoration-none fw-semibold">Tìm kiếm</a>

        <% if (currentUser) { %>
          <div class="dropdown">
            <button class="btn btn-light btn-sm dropdown-toggle" data-bs-toggle="dropdown">
              <%= currentUser.username %>
            </button>
            <ul class="dropdown-menu dropdown-menu-end">
              <li><a class="dropdown-item" href="/user/profile">Hồ sơ</a></li>
              <% if (currentUser.role === 'admin') { %>
                <li><a class="dropdown-item" href="/admin/dashboard">Trang quản trị</a></li>
              <% } %>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item text-danger" href="/auth/logout">Đăng xuất</a></li>
            </ul>
          </div>
        <% } else { %>
          <a href="/auth/login" class="btn btn-light btn-sm">Đăng nhập</a>
          <a href="/auth/register" class="btn btn-outline-light btn-sm">Đăng ký</a>
        <% } %>
      </nav>
    </div>
  </header>
